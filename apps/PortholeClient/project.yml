name: PortholeClient
options:
  bundleIdPrefix: app.porthole
  deploymentTarget:
    iOS: "26.0"
packages:
  Porthole:
    path: ../..
targets:
  PortholeClient:
    type: application
    platform: iOS
    deploymentTarget: "26.0"
    sources:
      - Sources
    dependencies:
      - package: Porthole
        product: PortholeTransport
      - package: Porthole
        product: PortholeMedia
      - package: Porthole
        product: PortholeProtocol
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: app.porthole.client
        INFOPLIST_KEY_NSLocalNetworkUsageDescription: "Porthole connects to your Mac on your local network to mirror its screen."
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        TARGETED_DEVICE_FAMILY: "1,2"
        CODE_SIGNING_ALLOWED: NO
schemes:
  PortholeClient:
    build:
      targets:
        PortholeClient: all
    run:
      config: Debug
