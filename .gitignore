# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Generated Xcode projects + Info.plists (regenerate with `xcodegen generate` in apps/*/)
apps/*/*.xcodeproj/
apps/*/*-Info.plist

# macOS
.DS_Store

# Brainstorm visual-companion scratch (not source)
.superpowers/

# Agent working state. These were previously masked only by machine-local files
# (~/.config/git/ignore, .git/info/exclude) which do NOT travel with a clone —
# ai-scratch/ in particular is the designated home for transient secrets.
ai-scratch/
.beads/
.pi-subagents/

# Claude Code workflow worktrees (transient; created by isolation: 'worktree')
.claude/worktrees/
.worktrees/

# Secrets / signing
*.p12
*.mobileprovision
.env

# Local device-test capture videos (not source)
*.mp4
*.MP4
*.mov
*.MOV
